# Overrides solo para `npm run build` (mode=production).
# Las claves de Firebase se heredan de .env.
VITE_API_URL=https://appingles-km24.onrender.com/api
VITE_AUTH_MODE=firebase

# Deploy en subcarpeta de Hostinger (prefijo de assets y del router).
# Ruta oficial: https://www.ingresosdigitalesit.com/appingles
VITE_BASE_PATH=/appingles/
